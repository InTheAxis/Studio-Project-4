using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImportDestructible
{
    //public void Import(string inputFilePath, string targetFileDir, bool isPlaceholder)
    //{
    //    DirectoryInfo dir = new DirectoryInfo(inputFilePath);
    //    FileInfo[] info = dir.GetFiles("*.*");

    //    foreach (FileInfo f in info)
    //    {
    //        //Debug.Log(f.Extension);
    //        string output = targetFileDir + "/";
    //        if (isPlaceholder)
    //            output += "Placeholder/";
    //        output += f.Name;

    //        FileUtil.CopyFileOrDirectory(f.ToString(), output);
    //    }

    //    //Debug.Log(targetFileDir);
    //}
}
